.jumbotron
  %h1
    Asset Managment

%h2
  User:
  = current_user.email if current_user
%h3
  Organization:
  = current_user.organization.name if (current_user && current_user.organization)
%br
%h4
  Assets:
  - current_user.organization.assets.each do |asset|
    = asset.name
- if current_user
  = link_to 'New Organization', new_organization_path
  %br
  = link_to 'New Asset', new_asset_path if current_user.organization
- else
  Sign up to create an organization
%h5 
  Member:
  = current_user.member.email if current_user
%br
= link_to 'Sign Up', new_user_registration_path
